class Config {
  /// Facebook App ID
  /// Required if using Facebook Auth
  static String facebookAppId = '';

  /// Facebook Version Number
  /// Required if using Facebook Auth
  static String facebookVersionNumber = '';
}
